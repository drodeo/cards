
<%= content_tag :div,
                id: "cards-form",
                data: {
                    cards: @cards,
                    user: current_user.to_json
                } do %>
 <% props = { user: current_user}.to_json
%>
  <Card :cards="cards"></Card>

<% end %>
<%= javascript_pack_tag 'card' %>
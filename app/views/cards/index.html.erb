
<%= content_tag :div,
                id: "cards-form",
                data: {
                    cards: @cards,
                    user: current_user.to_json
                } do %>

  <Card :cards="cards" :current_user="current_user"></Card>

<% end %>
<%= javascript_pack_tag 'card' %>